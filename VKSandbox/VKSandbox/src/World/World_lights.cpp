#include "World.h" 

namespace World {
    
   // void AddLight(LightCreateInfo createInfo) {
   //     std::vector<Light>& lights = GetLights();
   //     lights.push_back(Light(createInfo));
   // }
   //
   // Light* GetLightByIndex(int32_t index) {
   //     std::vector<Light>& lights = GetLights();
   //     if (index >= 0 && index < lights.size()) {
   //         return &lights[index];
   //     }
   //     else {
   //         return nullptr;
   //     }
   // }
}